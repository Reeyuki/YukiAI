{
	"name": "chat-application",
	"version": "1.0.0",
	"description": "Modular chat application with audio recording",
	"type": "module",
	"main": "main.js",
	"scripts": {
		"format": "npx prettier . --write",
		"build": "esbuild main.js --bundle --outfile=dist/bundle.js --format=esm",
		"build:watch": "esbuild main.js --bundle --outfile=dist/bundle.js --format=esm --watch",
		"build:prod": "esbuild main.js --bundle --outfile=dist/bundle.js --format=esm --minify"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"esbuild": "^0.19.0"
	}
}
